[project]
name = "advent-of-code"
version = "0.1.0"
description = "Advent of Code solutions in multiple languages"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.uv.sources]
